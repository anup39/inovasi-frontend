import MapContainer from '../components/MapContainer';
import Layout from './Layout';

function MapOnly() {
  return (
    <Layout>
      <MapContainer />
    </Layout>
  );
}
export default MapOnly;
